# AI-scouting
Build a prototype that automates the scouting of accelerators and startups in Europe and generates a concise value proposition for startups, using Google Sheets + Apps Script (and an LLM via API).
